spring:
  application:
    name: tms-backend

  datasource:
    url: jdbc:mysql://localhost:3306/tms2_db
    username: root
    password: mann1105
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

server:
  port: 8080